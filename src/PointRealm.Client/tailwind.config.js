/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ["class"],
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        // PointRealm Theme Tokens
        pr: {
          bg: "var(--pr-bg-dark)",
          surface: "var(--pr-surface-slate)",
          "surface-2": "var(--pr-surface-elevated)", // Mapped to elevated
          text: "var(--pr-text-primary)",
          "text-muted": "var(--pr-text-muted)",
          primary: "var(--pr-primary-cyan)",
          secondary: "var(--pr-secondary-gold)",
          success: "var(--pr-success)",
          danger: "var(--pr-danger)",
          border: "var(--pr-border-color)",
          shadow: "var(--pr-shadow-md)",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      fontFamily: {
        heading: ["var(--pr-heading-font)", "serif"],
        body: ["var(--pr-body-font)", "sans-serif"],
        mono: ["var(--pr-mono-font)", "monospace"],
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
      boxShadow: {
        'soft': 'var(--pr-shadow-soft)',
        'glow-primary': 'var(--pr-primary-glow)',
        'glow-secondary': 'var(--pr-secondary-glow)',
      }
    },
  },
  plugins: [require("tailwindcss-animate")],
}
