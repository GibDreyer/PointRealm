# Backend URL for Vite Proxy (Development only)
# Using 127.0.0.1 instead of localhost to avoid IPv6 resolution issues (ECONNREFUSED)
VITE_BACKEND_URL=http://127.0.0.1:5219

# API and SignalR Hub URLs
# In dev, relative paths leverage the Vite proxy (removes CORS issues)
# In production, set these to absolute URLs if the API is on a different domain
VITE_API_BASE_URL=/api/v1
VITE_SIGNALR_HUB_URL=/hubs/realm

VITE_BUY_ME_COFFEE_URL=
